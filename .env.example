WEBADDRESS=127.0.0.1
WEBPORT=9955
UPLOAD_DIR=/srv/upload
AUTH_DB=/opt/ipd/users.txt

# Per-step control script timeouts, seconds (default 1800)
#CHECK_TIMEOUT=300
#PREPARE_TIMEOUT=1800
#DEPLOY_TIMEOUT=1800

# Max time to wait for running deployments on shutdown, seconds
#SHUTDOWN_TIMEOUT=60
